using UnityEngine;

namespace Com.ThirdNerve.Backfire.Runtime.Player
{
    public class ReflectorDataComponent : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
